import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    main: {
        backgroundColor: '#569cbc',
        height: '100%'
    },
    buttonGroup: {
        flex: .5
    },
    buttonText: {
        color: "#fff"
    }
});